/**
 * @param {Array.<MdastNode>} nodes
 */
export function wrap(nodes: Array<MdastNode>): import('mdast').Content[]
/**
 * Check if there are non-phrasing mdast nodes returned.
 * This is needed if a fragment is given, which could just be a sentence, and
 * doesn’t need a wrapper paragraph.
 *
 * @param {Array.<MdastNode>} nodes
 * @returns {boolean}
 */
export function wrapNeeded(nodes: Array<MdastNode>): boolean
export type H = import('../types.js').H
export type MdastNode = import('../types.js').MdastNode
export type MdastPhrasingContent = import('../types.js').MdastPhrasingContent
